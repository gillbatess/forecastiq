# All optional - the defaults work out of the box.
LOG_LEVEL=INFO
# Only needed if you host a separate front end on another domain (comma separated):
# CORS_ORIGINS=https://app.example.com
# Retraining: where the Kaggle CSVs live / where artifacts are written
# DATA_DIR=data/raw
# ARTIFACT_DIR=artifacts
